import pandas as pd
import numpy as np

# --- Generate mock real_sales_data.csv with 512 features + lat/lon + sales ---
def generate_valid_real_data_csv(filepath="real_sales_data.csv", num_rows=100):
    feature_data = np.random.rand(num_rows, 512)
    lat_lon = np.random.uniform(low=[25, -125], high=[49, -66], size=(num_rows, 2))  # USA lat/lon
    sales = np.random.randint(5000, 50000, size=(num_rows, 1))

    data = np.hstack([feature_data, lat_lon, sales])
    columns = [f"f{i}" for i in range(512)] + ["lat", "lon", "sales"]
    df = pd.DataFrame(data, columns=columns)
    df.to_csv(filepath, index=False)

# Call this once to regenerate a clean, valid CSV file
generate_valid_real_data_csv()

