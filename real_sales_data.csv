import pandas as pd
import numpy as np

# --- Simulate real sales dataset ---
def generate_real_sales_data(filename="real_sales_data.csv", num_rows=100):
    np.random.seed(42)
    features = np.random.rand(num_rows, 512)
    lat = np.random.uniform(25.0, 49.0, size=(num_rows, 1))
    lon = np.random.uniform(-124.0, -66.0, size=(num_rows, 1))
    sales = np.random.randint(5000, 50000, size=(num_rows, 1))

    data = np.hstack([features, lat, lon, sales])
    cols = [f"f{i}" for i in range(512)] + ["lat", "lon", "sales"]
    df = pd.DataFrame(data, columns=cols)
    df.to_csv(filename, index=False)
    print(f"✅ {filename} created with {num_rows} rows.")

# --- Run ---
generate_real_sales_data()
