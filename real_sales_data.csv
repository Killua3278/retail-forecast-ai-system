import pandas as pd
import numpy as np

# --- Configuration ---
num_samples = 10
num_features = 512

# --- Generate dummy features (f0 to f511) ---
data = {
    f"f{i}": np.random.rand(num_samples) * 100 for i in range(num_features)
}

# --- Add lat, lon, and sales columns ---
data["lat"] = np.random.uniform(25.0, 50.0, size=num_samples)   # USA latitude range
data["lon"] = np.random.uniform(-125.0, -70.0, size=num_samples)  # USA longitude range
data["sales"] = np.random.uniform(1000, 50000, size=num_samples)

# --- Convert to DataFrame ---
df = pd.DataFrame(data)

# --- Save as real_sales_data.csv ---
df.to_csv("real_sales_data.csv", index=False)
print("✅ real_sales_data.csv created with shape:", df.shape)
